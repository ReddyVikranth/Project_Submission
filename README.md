# Contract Intelligence Parser - Sample4

Improved frontend with Tailwind UI and a "View Results" flow.

Run instructions same as sample3. Frontend uses Tailwind — run `npm install` in `frontend/` before `npm run dev`.
